{
  "name": "gorillas-bas",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
